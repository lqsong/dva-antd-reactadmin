import React from 'react';
import { connect } from 'dva';

const Products = () => {

    return (
        <div>
            <h2>List of add</h2>
        </div>
    );
};

export default connect()(Products);